{{- $gatewayApi := .Values.query.gatewayApi | default dict -}}
{{- $httpRouteSpec := $gatewayApi.httpRoute | default dict -}}
{{- $hostnames := $httpRouteSpec.hosts | default (list (include "query.ingress" .)) -}}
{{- $name := printf "%s-query" .Values.jaeger.serviceName -}}
{{- $install := or ($httpRouteSpec.install | default false) (and .Values.query.ingress.install (include "jaeger.gatewayApi.enabled" .)) -}}
{{- if and .Values.query.install $install }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ $name }}
  labels:
    app.kubernetes.io/instance: {{ cat .Values.jaeger.serviceName "-query-" .Release.Namespace | nospace | trunc 63 | trimSuffix "-" }}
    {{- include "jaeger.labels" (dict "ctx" . "name" $name "component" "query") | nindent 4 }}
    {{- if $httpRouteSpec.labels }}
      {{- toYaml $httpRouteSpec.labels | nindent 4 }}
    {{- end }}
  {{- if $httpRouteSpec.annotations }}
  annotations:
    {{- toYaml $httpRouteSpec.annotations | nindent 4 }}
  {{- end }}
spec:
  parentRefs:
    {{- include "jaeger.gatewayApi.parentRefs" (dict "ctx" $ "parentRefs" $httpRouteSpec.parentRefs) | nindent 4 }}
  {{- if $hostnames }}
  hostnames:
    {{- include "jaeger.gatewayApi.hostnames" (dict "ctx" $ "hosts" $hostnames) | nindent 4 }}
  {{- end }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: "/"
      backendRefs:
        - name: {{ $name }}
          port: 16686
{{- end }}
